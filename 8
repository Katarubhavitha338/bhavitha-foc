#
    printf("Enter an integer: ");
    scanf("%d", &num);

    // true if num is perfectly divisible by 2
    if(num % 2 == 0)
        printf("%dinclude <stdio.h>
int main() {
    int num; is even.", num);
    else
        printf("%d is odd.", num);
    
    return 0;
}
